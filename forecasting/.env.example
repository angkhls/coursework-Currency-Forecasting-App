# SQLite по умолчанию (не нужен Docker)
STORAGE=sqlite
SQLITE_PATH=./data/currency.db

# Или PostgreSQL:
# STORAGE=postgres
# DATABASE_URL=postgresql://currency_user:currency_pass@localhost:5432/currencydb

CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
